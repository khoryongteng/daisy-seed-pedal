x-common: &common
  build:
    context: .
    dockerfile: Dockerfile
  volumes:
    - .:/workspace
  working_dir: /workspace

services:
  workspace:
    <<: *common
    command: /bin/bash
    stdin_open: true
    tty: true

  build:
    <<: *common
    command: echo "Please specify a build command"
